export interface Activation {
    id: number,
    activationKey: string,
    date: string,
    expired: string,
    ipAddress: string,
    license_name: string,
    macAddress: string,
    status: any
}